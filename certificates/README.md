* Utilizzare un file `csv` con una lista di nomi e cognomi separati da uno spazio
* Il file `main.tex` è il template
* Il file `build_certificates.sh` genera un file per ogni persona
* Compilare due volte il file tex!!!
